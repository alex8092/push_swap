#include "push_swap.h"

void				ft_ps_reader_parse_arg(char **av, int ac)
{
	t_ps			*ps;
	int				i;

	ps = ft_ps_get_instance();
	ps->b = ft_dlist_new();
	ps->a = ft_dlist_new();
	i = 1;
	while (av[i])
	{
		
	}
}